<UserControl x:Class="wpfGMTraceability.UserControls.TitleChangeDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:wpfGMTraceability.UserControls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Width="400" Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="¿Cómo se llama esta estación?"
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="{DynamicResource PrimaryHueMidBrush}"
                   Margin="0,0,0,12"/>

        <TextBox x:Name="txtWindowName"
                 Grid.Row="1"
                 Margin="0,0,0,12"
                 materialDesign:HintAssist.Hint="Nombre de la estacion"
                 FontSize="16"
                 Padding="8"/>

        <Button Grid.Row="2"
                Content="Aceptar"
                Style="{StaticResource MaterialDesignRaisedButton}"
                HorizontalAlignment="Right"
                Padding="12,6"
                x:Name="btnOk" Click="btnOk_Click"/>
    </Grid>
</UserControl>
