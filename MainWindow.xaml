<Window x:Name="Main_Window" x:Class="wpfGMTraceability.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:wpfGMTraceability"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"        
        mc:Ignorable="d" WindowState="Maximized"
        WindowStyle="None" ResizeMode="NoResize" Height="720" Width="1280" Background="{x:Null}" AllowsTransparency="True" Loaded="Main_Window_Loaded" Closing="Main_Window_Closing">
    <Grid>
        <materialDesign:Card Margin="0" UniformCornerRadius="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="70"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2.5*" />
                        <ColumnDefinition Width="0.5*" />
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Column="0" Grid.ColumnSpan="2" Margin="0 0 0 0">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBlock FontWeight="ExtraBold" FontSize="14" Text="&#xF06C;" FontFamily="wingdings" VerticalAlignment="Center" Foreground="Green" />
                            <TextBlock x:Name="txtBTitle" Text="ESTACION #5 - DESFORRE" TextWrapping="Wrap" HorizontalAlignment="Right" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="30" Margin="5 0 10 0" />
                        </StackPanel>
                    </Grid>
                    <Grid Grid.Column="1" Margin="0 0 10 0">
                        <Button Style="{StaticResource MaterialDesignFloatingActionButton}" x:Name="OpenOptions" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="Black" Click="OpenOptions_Click" HorizontalAlignment="Right" VerticalAlignment="Center">
                            <StackPanel >
                                <materialDesign:PackIcon Margin="0 0 6 0" Width="56" Height="56" Kind="ViewHeadline" />
                            </StackPanel>
                        </Button>
                    </Grid>
                </Grid>
                <Grid Grid.Row="1" Background="#F6F5F8" Name="RenderPages"></Grid>
            </Grid>
        </materialDesign:Card>

        <!-- Overlay oscuro -->
        <Grid x:Name="OverlayOscuro" Background="#80000000" Visibility="Collapsed" Panel.ZIndex="99" />
    </Grid>
</Window>
